apiVersion: v1
kind: Service
metadata:
    name: yoletechdevopsdockercomposenodejsmysql
spec:
    type: LoadBalancer
    ports:
    - port: 8080 
    selector:
        app: yoletechdevopsdockercomposenodejsmysql